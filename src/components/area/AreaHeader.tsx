import React from 'react';
import { CardHeader, CardTitle } from "@/components/ui/card";

export const AreaHeader: React.FC = () => (
  <CardHeader>
    <CardTitle>Area & Task Details</CardTitle>
  </CardHeader>
);